# Terraform-and-basic-variables
It includes examples of defining variables with default values, providing values via -var or terraform.tfvars, and interpolating those variables in resource definitions.
